# flux1

Generate an image using Black Forest Labs FLUX 1.1 [pro], the generation before
FLUX.2. It is not retired — the model is still supported, and it is cheaper than
FLUX.2 [max], so this is a choice rather than a fallback.

**Usage:** `z flux1 <prompt>`

For the current top-tier model, see `z flux`.
