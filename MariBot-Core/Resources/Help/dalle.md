# dalle

**Retired.** OpenAI has discontinued DALL-E 3. The command still generates: it
warns that the model is gone and then runs your prompt on `z gptimage`'s model
instead.

**Usage:** `z dalle <prompt>`

Also available as `z dallehd`.
